<% for (let i=0; i < post.length; i++) { %>
    <% if (post[i].composeTitle && post[i].composePost) { %>
        <h1>
            <%= post[i].composeTitle %>
                <% console.log(post[i].composeTitle) %>
        </h1>
        <p>
            <%= post[i].composePost %>
        </p>
        <%} else { %>
            <h1>Nothing</h1>
            <% } } %>