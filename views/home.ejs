<%- include('./partials/header') -%>
    <h1>
        Home
    </h1>
    <p>
        <%= homeStartingContent %>
    </p>

    <!-- 
    <% for (let i=0 ; i < post.length ; i ++ ) { %>
        <% console.log(post[i].composeTitle) %>
            <% } %>
 -->
    <% post.forEach( value=> { %>
        <h1>
            <%= value.composeTitle %>
        </h1>
        <p>
            <%= value.composePost.substring(0,100) + '...' %>
                <a href="posts/<%=value.composeTitle%>"> Read MORE</a>
        </p>
        <% }) %>

            <%- include('./partials/footer') -%>